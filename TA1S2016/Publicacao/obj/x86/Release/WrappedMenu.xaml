<Page
    x:Class="Publicacao.WrappedMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Publicacao"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Background="LightGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:ConnectionId='2' Name="btnMenu" Grid.Column="0"
                    BorderBrush="Black" BorderThickness="0,0,0,1"
                    FontFamily="Segoe MDL2 Assets"
                    Content="&#xE700;" Width="50"
                    Height="50"
                                           />
            <Border Grid.Column="1" BorderBrush="Black"
                    BorderThickness="0,0,0,1">
                <TextBlock Text="Meus Contatos"
                           VerticalAlignment="Center" Margin="10,0,0,0"
                           Style="{ThemeResource TitleTextBlockStyle}"/>
            </Border>
        </Grid>

        <SplitView x:ConnectionId='1' Grid.Row="1" x:Name="MySplitView"
                   DisplayMode="Overlay"
                   IsPaneOpen="True"
                   CompactPaneLength="0"
                   OpenPaneLength="260">
            <SplitView.Pane>
                <ListView SelectionMode="None">
                    <ListViewItem HorizontalContentAlignment="Stretch">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0"
                                    Margin="12"
                                    BorderBrush="Silver" BorderThickness="1"
                                    Height="96" Width="96" CornerRadius="50"
                                    HorizontalAlignment="Center">
                                <Border.Background>
                                    <ImageBrush ImageSource="/Assets/Unknown_Character.png"
                                                Stretch="Uniform"/>
                                </Border.Background>
                            </Border>

                            <TextBlock Grid.Row="1"
                                       Text="Unknown Character"
                                       TextAlignment="Center"
                                       TextTrimming="WordEllipsis"/>
                        </Grid>
                    </ListViewItem>
                    <ListViewItem>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE713;"
                                       FontSize="18" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center"/>
                            <TextBlock Text="Configurações"
                                       FontSize="18" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ListViewItem>
                </ListView>
            </SplitView.Pane>
        </SplitView>
    </Grid>
</Page>

