<Page
    x:Class="DataBinding.DataBindingObjetos"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DataBinding"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:modelos="using:DataBinding"
    mc:Ignorable="d">

    <Page.DataContext>
        <modelos:Contato Nome="Fulano" Sobrenome="de Tal"/>
    </Page.DataContext>
    

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBox Header="Nome"
                 Grid.Row="0"
                 Text="{Binding Path=Nome}"
                 Margin="12"/>

        <TextBox Header="Sobrenome"
                 Grid.Row="2"
                 Text="{Binding Path=Sobrenome}"
                 Margin="12"/>

    </Grid>
</Page>
